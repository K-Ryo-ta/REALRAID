import React from "react";

const SendWordButton = ({
  onClick,
}: {
  onClick: (e: React.FormEvent<HTMLFormElement>) => void;
}) => {
  return <div>SendWordButton</div>;
};

export default SendWordButton;
